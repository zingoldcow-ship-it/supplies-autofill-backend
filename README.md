# 아이스크림몰 장바구니 엑셀 → 자동 변환 (Streamlit)

- 아이스크림몰 장바구니/견적서 엑셀(.xlsx)을 업로드하면
- `품목/규격/수량/단가(정가)/단가(할인)/금액(정가)/최종금액/상품코드/사이트` 형태로 정규화한 뒤
- 결과를 웹에서 확인하고 엑셀로 다운로드할 수 있습니다.

## 실행(로컬)
```bash
pip install -r requirements.txt
streamlit run app.py
```
